#pragma once

#include <cstddef>

/// The largest possible country code
static constexpr size_t max_country_code = 999;
/// The number of bits required to store a country code
static constexpr size_t country_code_bits = 10;
 